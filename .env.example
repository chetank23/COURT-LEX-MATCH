# LexMatch AI - Environment Configuration
# Copy this file to .env and fill in your values.

# SQLite database file path (relative to project root or absolute)
# Default: data/lexmatch.db  (auto-created on first run)
# DATABASE_PATH=data/lexmatch.db

# API server port
PORT=4000

# Node environment
NODE_ENV=development
